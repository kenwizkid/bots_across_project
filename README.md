# bots_across_project
This is a placeholder project to apply across.to api 

My project is to apply across.to api, for an internal cross exchange abitrager, which requires cross chain margin movement, usually arbitrum USDC <-> robinhood USDG

Low QPS, self use in production, so I can't use the endpoints without an integrator ID. 

